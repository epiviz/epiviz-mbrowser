{
  "dependencies": {
    "polyserve": "^0.27.15"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-string": "^1.5.0",
    "mocha": "^8.2.1",
    "polymer-cli": "^1.9.11",
    "puppeteer": "^5.5.0"
  },
  "scripts": {
    "test": "mocha --timeout 0  ./test/bootstrap.js --recursive test",
    "test-brk": "mocha --inspect-brk --timeout 0  ./test/bootstrap.js --recursive test",
    "test-1": "mocha --timeout 0  ./test/bootstrap.js --recursive test  -g",
    "test-1-brk": "mocha --inspect-brk --timeout 0  ./test/bootstrap.js --recursive test -g"
  }
}
